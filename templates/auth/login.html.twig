{% extends('base.html.twig') %}
{% block title %}
    Register New User
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href={{ asset('css/form.css') }}>
{% endblock %}
{% block body %}
    <main class="center">
        <div class="form shadow">
            <div class="form__switch-form">
                <a  href={{ path('register') }}>SIGN UP</a>
                <a class="active-form" href={{ path('login') }}>LOGIN</a>
            </div>
            {{ form(form) }}
        </div>
    </main>


{% endblock %}